# Pinned to an exact smart-spatial-system release so results in this
# repository stay reproducible as that project keeps developing. Bump this
# deliberately - say in the commit message which upstream release it moves
# to and which bugs/ report it closes, and re-run every phase whose outputs
# could change. Never loosen it to ">=" for day-to-day work.
#
# INSTALL SOURCE (confirmed 2026-09-19): v0.3.0 is a real tag at
# github.com/arazshah/smart_spatial_system (pyproject.toml's version field
# reads "0.3.0" at that tag) - confirmed by cloning it directly, which this
# session could not do the first time it tried (see git history) but could
# once network access was widened. PyPI itself, however, stayed blocked
# (403) even after that - `pip install` cannot resolve build dependencies
# from here, so this pin is verified-real but still unverified-installable
# in this environment. Use the tag install below until someone confirms the
# plain pin resolves on PyPI from an environment that can actually reach it:
#
#   pip install "smart-spatial-system[raster,pdf] @ git+https://github.com/arazshah/smart_spatial_system.git@v0.3.0"
#
# Delete this note once `pip install -r requirements.txt` has actually been
# run successfully somewhere and the plain pin below is confirmed to work.
smart-spatial-system==0.3.0

# Required by the task spec, and used directly by the data-prep and
# rule-based phases.
geopandas
requests

# Notebook environment
jupyterlab>=4.0
ipykernel>=6.29

# Analysis
pandas>=2.2
numpy>=1.26
scipy>=1.13          # spearmanr for the Rank Stability metric

# Mapping / plotting
matplotlib>=3.8
folium>=0.16

# The LLM arm needs nothing beyond what smart-spatial-system already pulls
# in (its own OpenAI-compatible client) - no extra client library required.
python-dotenv>=1.0
